УСТАНАВЛИВАЕШЬ VS CODE
папки node_modeles и файл package-lock.json (если они останутся) удаляешь совсем
в терминале забиваешь npm install (подтянет зависимости)

ПОТОМ КОГДА ЗАХОЧЕШЬ СОБРАТЬ В ТЕРМИНАЛЕ ВБИВАЕШЬ 
npm install 
он тебе в ответе покажет ссылку на локалхост, по ней переходишь и тестишь



# Человек против генетического алгоритма на основе игры "Быки и коровы"

Интерактивная веб-игра с визуализацией работы генетического алгоритма.

## Описание

Логическая игра "Быки и Коровы" с противником в виде компьютера на основе генетического алгоритма. Игра демонстрирует, как генетический алгоритм может решать комбинаторные задачи.

### Правила игры:
- Загадывается 4-значное число с уникальными цифрами (0-9)
- **Бык** — правильная цифра на правильном месте
- **Корова** — правильная цифра на неправильном месте

### В игре присутсвует два режима:
1. **Человек vs ГА** — вы загадываете число, генетический алгоритм пытается угадать
2. **Человек угадывает** — компьютер загадывает, вы отгадываете

## Генетический алгоритм

Реализован полноценный ГА с:
- **Популяцией** из 100 особей
- **Функцией приспособленности** на основе согласованности с предыдущими попытками
- **Турнирной селекцией** (турнир из 5 особей)
- **Одноточечным скрещиванием** с исправлением дубликатов
- **Мутацией** (15% вероятность свапа цифр)
- **Элитизмом** (сохранение 10 лучших особей)
- **Разнообразием** (5% случайных особей каждое поколение)

## Использованные технологии

- **React 18** + **TypeScript** — современный UI
- **Tailwind CSS 4** — стилизация
- **Vite** — быстрая сборка
- **Recharts** — визуализация (графики)
- **Lucide React** — иконки

## Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build
```
4. Настройки сборки:
   - **Build command:** `npm run build`
   - **Publish directory:** `dist`
5. Нажмите "Deploy site"

### Вариант 2: Через Netlify CLI

```bash
# Установка Netlify CLI
npm install -g netlify-cli

# Логин
netlify login

# Инициализация и деплой
netlify init

# Или ручной деплой
npm run build
netlify deploy --prod
```

## Структура проекта

```
src/
├── app/
│   ├── App.tsx                    # Главный компонент
│   ├── components/
│   │   ├── GameBoard.tsx          # Игровая доска
│   │   ├── HumanVsGAPanel.tsx     # Режим: Человек vs ГА
│   │   ├── HumanGuessingPanel.tsx # Режим: Человек угадывает
│   │   ├── GAPanel.tsx            # Визуализация ГА
│   │   ├── GuessHistory.tsx       # История попыток
│   │   ├── EducationalPanel.tsx   # Обучающая панель
│   │   ├── AboutPanel.tsx         # Информация об игре
│   │   └── ModeSelector.tsx       # Выбор режима
│   └── utils/
│       ├── gameLogic.ts           # Логика игры
│       └── geneticAlgorithm.ts    # Реализация ГА
└── styles/
    ├── index.css                  # Основные стили
    └── theme.css                  # CSS переменные

netlify.toml                       # Конфигурация Netlify
```

## Как работает ГА

1. **Инициализация:** Создается популяция из 100 случайных 4-значных чисел
2. **Оценка:** Каждая особь оценивается по согласованности с историей попыток
3. **Селекция:** Турнирный отбор лучших кандидатов
4. **Скрещивание:** Комбинирование генов родителей с точкой разреза
5. **Мутация:** Случайный обмен позиций цифр (15% вероятность)
6. **Элитизм:** Перенос 10 лучших особей в новое поколение
7. **Эволюция:** Процесс повторяется до 100 поколений или пока не найдено решение

## Автор игры:

**Уфимцева Алиса Александровна**  
Студентка 4 курса ОФ-423/095-4-1 ИСиТ

---
